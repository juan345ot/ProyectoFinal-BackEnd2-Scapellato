<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="description" content="Carrito de compras del ecommerce. Administra tus productos aquí.">
  <title>Carrito - {{cartId}}</title>
  <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
  <header>
    <h1>Carrito de Compras</h1>
  </header>

  <main>
    <section id="cart-section">
      <h2>Carrito ID: {{cartId}}</h2>
      <ul id="cart-product-list" data-cart-id="{{cartId}}">
        {{#each cartProducts}}
          <li data-product-id="{{this.product._id}}">
            <strong>{{this.product.title}}</strong> - 
            <a href="/products/{{this.product._id}}">Ver Detalles</a>
            <div>
              <label for="quantity-{{this.product._id}}">Cantidad:</label>
              <input type="number" id="quantity-{{this.product._id}}" class="quantity-input" min="1" value="{{this.quantity}}">
            </div>
            <span class="unit-price">Precio unitario: ${{this.product.price}}</span>
            <span class="subtotal">Subtotal: ${{multiply this.quantity this.product.price}}</span>
            <button class="delete-product" data-product-id="{{this.product._id}}">Eliminar</button>
          </li>
        {{/each}}
      </ul>

      <div id="cart-total-container"></div>
      
      <div class="actions">
        <button id="empty-cart-button" data-cart-id="{{cartId}}">Vaciar Carrito</button>
        <a href="/?limit={{limit}}&page={{currentPage}}&sort={{sort}}&query={{query}}" class="back-to-home">Volver al Home</a>
      </div>
    </section>
  </main>

  <footer>
    <p>&copy; 2024 Ecommerce Server. Todos los derechos reservados.</p>
  </footer>

  <script src="/js/cart.js"></script>
</body>
</html>
